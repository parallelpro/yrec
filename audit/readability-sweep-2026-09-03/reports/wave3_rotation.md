## NAME = rotation

Worktree `/Applications/YREC-wt/rotation`, branch `rs/rotation`. This is batch **R5 (structural)** for the rotation domain. Read first: audit `rotation.md` ("Structural recommendations" 1, 3, 4; audit items 3, 4, 6, 20), `reports/rotation-wave1.md` (Skipped: the four CZ-extent copies "differ in their gating", `solve_r0` `.le.` vs `.lt.`), `reports/rotmix-wave2.md` and `reports/sidechan-wave2.md` (microdiff `*_bl` copies, gradient argument, `am_advection_diffusion_coeffs` items -- whatever they left under Deferred that is rotation-only is yours), `SUMMARY.md` section 3 (R5).

You own `src/rotation/**`. The `mixburn` agent replaces the scalar `rate_*` relay in `mixing/mix.f90`/`rotmix.f90` by a vector fill of `rot_scr%reaction_rate_by_zone(:,k)`; it may add or rename nothing in `rotation_scratch_lib.f90` except a comment on that array's row layout -- that file is **shared, hunk-disjoint**: you do not edit the `reaction_rate_by_zone` / `rr_*` lines. Nobody else touches rotation/.

### Items, in order

1. **Equal-grid toolkit -- the identical part only** (rotation Structural 1). `seculr/am_transport_grid.f90` and `seculr/composition_grid.f90` build an equally-spaced grid, interpolate edge coefficients through `osplin` using `rot_scr%xtab/ytab/xval/yval` as scratch, and evaluate a dchi/dr Jacobian. `diff -w` them: wave 1 found the four CZ-extent searches differ in gating (the i1 fix is in only one), so those are NOT shared. Extract into a new `rotation/seculr/equal_grid_lib.f90` (module `equal_grid_lib`, private to rotation) only the blocks that are token-identical between the two files after renaming (candidates: the `xtab` midpoint construction, the `ytab` fill + `osplin` call, the Jacobian evaluation) as subroutines taking the scratch arrays as explicit arguments instead of reading `rot_scr%`. Each extracted block must run in the same position in the caller with the same inputs. List every non-identical block with its difference under Deferred (the i1 gating differences are an author decision, R6).

2. **Dead-code purge with byte parity** (rotation Structural 3): `mid_timestep_model.f90` (the audit's 197-352 block -- grep for its current location), `evolve_angular_momentum.f90` BUR-ST branches, `viscos.f90` Endal-Sofia arithmetic, `am_advection_diffusion_coeffs.f90` `residual_check/coeff_matrix_orig/damping_factor/theta_term`, `banded_solver.f90` (audit 114-152), `diffuse_composition.f90` `dcomp/sum_species`, `microdiff_mte.f90` debug print, `zone_moments_of_inertia.f90` `prev_log_*/spherical_moment_of_inertia`, `rotation_shape_factors.f90` `aintt`, `circulation_velocities.f90` commented F77. Wave 1 removed some of these already -- check each; for each remaining one show it dead (no reader of the result, or unreachable by a constant condition that is not a namelist flag), delete, gate1. A block guarded by a `star%ctrl%`/`star%job%` flag is NOT dead even if no pin sets the flag: leave it and list it. The BS-extrapolation path (`evolve_angular_momentum.f90` ~104, 353-446, `rotation_stability_setup.f90` ~458-480, `difad_shear_coeff2` consumers) is an author decision -- do not touch.

3. **Species-descriptor tables** (rotation Structural 4, audit items 6/20): `viscos.f90`, `microdiff/gravitational_settling_setup.f90`, `microdiff/microdiff_coefficients.f90` each carry their own (name, A, Z) species lists, and `microdiff.f90`/`microdiff_etm.f90` address species by bare `1..11`. Add one `species_props` parameter table (module in rotation/, e.g. in `rotation_scratch_lib.f90` or a new `rotation/species_table_lib.f90`) and make the three setup routines read it -- only where the three lists agree value-for-value (diff them first; a differing A or Z means the local list stays and the difference is reported). Replace bare species subscripts by the `i_*` names from `star_info_lib` where the mapping is proven by the writer. Do not build the per-species diffusion descriptor loop (it reorders the per-species blocks; class B risk) -- list it under Deferred.

4. **`am_advection_diffusion_coeffs` cleanups** (audit item 17, sidechan-wave2 leftovers): if sidechan deferred the `timestep` mutate-then-restore and `wind_loss_implicit` leak as not byte-safe, leave them; do the parts it deferred as "rotation-only, safe" (local `json/nmax` shadowing -> module parameters if same values; unread computed locals deleted after grep).

5. **`solve_r0` Newton loops** (wave-1 Skipped, `.le.` vs `.lt.`): keep both; add a one-line comment at each pointing at the other and the comparison operator difference so a later reader does not re-discover it.

### Files you may edit

`src/rotation/**` except the `reaction_rate_by_zone` / `rr_*` lines of `rotation_scratch_lib.f90`; `src/tools/check_boundaries.py` (minimal allowlist edits only, e.g. registering `equal_grid_lib` as rotation-internal); `src/deps.mk`.
