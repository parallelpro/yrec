## NAME = state

Worktree `/Applications/YREC-wt/state`, branch `rs/state`. Read first: `reports/burn-wave1.md`, `reports/core-wave1.md`, `reports/mixwind-wave1.md` (items 1, 2, 7, 8), `reports/rotation-wave1.md` (item 4), and audit `core.md`, `burn.md`.

You own the shared state modules (`src/state/star_info_lib.f90`, `src/state/phys_const_lib.f90`) and the cross-domain items whose fix starts there. Another agent (`rotmix`) owns `src/rotation/rotation_scratch_lib.f90` and rotation/mixing renames; another (`ioctrl`) owns `src/io/controls_lib.f90` and everything namelist-facing. Do not touch those.

### Items, in order

1. **`star%reaction_rate_1..13(json)` -> `star%reaction_rate(13,json)`** (burn report item 1). Add `n_reactions = 13` (or import `num_reactions` from net_lib if state/ may depend on net/ -- check `tools/check_boundaries.py`; if not, a same-value parameter in star_info_lib with a comment) and replace the thirteen members by one 2-D array. Writers in `core/burn_lib.f90` (compute_neutrino_emission): keep one assignment per element in the same order, or an array-section assignment of the same product. Readers: grep `reaction_rate_[0-9]` and every other consumer of those members (io/ history/profile writers may address them by name -- check `io/` and `util/`). If any reader is in a file owned by `ioctrl` (io/*), stop: leave the member names as they are and defer the whole item; do not half-migrate.
2. **`i_nu_he3he3 = 9`, `i_nu_he3he4 = 10`** next to `i_nu_f17` in star_info_lib; fix the "spare slots" comment; replace the two literal subscripts in `core/burn_lib.f90` (burn item 2).
3. **`gyr_amu_per_sec_gram`** -> `double precision, parameter :: gyr_amu_per_sec_gram = 5.240358d-8` in `src/state/phys_const_lib.f90`; delete the copy in `src/net/net_lib.f90`; users (`core/burn_lib.f90`, net_lib routines) import from phys_const_lib (burn item 4).
4. **Rotation's literal physical constants into phys_const_lib** (rotation report item 4) -- add `m_proton_cgs = 1.6726d-24`, `rsun_cgs_legacy = 6.9598d10` (check first whether phys_const_lib already has an `rsun`-like constant with a *different* value; if so use a distinct name and a comment saying why the two coexist), `amu_cgs_legacy = 1.6605655d-24` (same check), `m_electron_amu = 5.486d-4`, and whatever `6.7282653d-26` and `5.669d-5` are (identify them from context; name accordingly), **with exactly these values**. Replace the literals at `rotation/microdiff/gravitational_settling_setup.f90:270,71,66`, `rotation/microdiff/microdiff_coefficients.f90:126,74`, `rotation/microdiff/microdiff_setup.f90:65`, `rotation/viscos.f90:25,66,101` (line numbers are pre-wave-1; grep the literals). These four rotation files are *shared, hunk-disjoint* with `rotmix`: change only the literal-constant lines.
5. **Named counts in star_info_lib** (core report items 1-4): `n_species_basic = 11`, `n_species_extended = 15` (next to the `i_*` species slots), `n_lum_channels = 8`, `n_nu_fluxes = 10`, `max_mc_runs = 1000`; use them for the declarations and at the literal sites in `core/henyey_iterate.f90`, `core/evolve_step.f90`, `core/read_starting_model.f90`, `core/rebuild_envelope.f90`, `core/henyey_coefficients.f90`, `core/observables_lib.f90`, `core/neutrino_flux_table.f90`, `core/monte_carlo.f90`. Do not change loop bounds that are not literally the same number (e.g. a `do k = 4,...` stays unless `4` provably is `i_he3`, in which case say so in the commit message).
6. **`max_convective_zones = 12`, `max_radiative_zones = 13`** in star_info_lib (mixwind item 7) for `convective_zone_bounds(12,2)`/`radiative_zone_bounds(13,2)`/`mixed_zone_bounds_no_overshoot(13,2)`; use them in the declarations and at the literal sites in `mixing/find_convection_zones.f90`, `setup/locate_shell_boundaries.f90`; and fix the mismatched dummy `mixed_zone_bounds_no_overshoot(12,2)` in `mixing/mix.f90:48` to the actual `(13,2)` (core item 5).
7. **`ln_solar_luminosity` -> `ln10_over_lsun`** (mixwind item 8): member in star_info_lib and the three sites `setup/setups.f90`, `core/monte_carlo.f90`, `core/henyey_coefficients.f90`. Pure rename; check `grep -rn ln_solar_luminosity src/` is empty afterwards (io/ readers too -- if an io/ file uses it, defer).
8. **`hydrogen_dt` -> `chosen_dt`** (mixwind item 2): member in star_info_lib; sites in `core/evolve_step.f90`, `core/run_yrec.f90`, `util/compute_timestep.f90`, `util/timestep_limit_hburn.f90` (verify the mixwind report's claim that nothing reads the value as an H-burn governor before renaming; if `timestep_limit_hburn` *does* read it as one, the rename is wrong -- skip and report). Rewrite the note at `core/main.f90:82-86` accordingly.
9. **Drop the unused `age_gyr` argument of `massloss`** (mixwind item 1): `core/evolve_step.f90` call and the `wind/massloss.f90` dummy. Show it unread (grep the routine and every contained procedure).
10. **`laol_work_array` element meanings** (kapatm item 9): the 12-element mixture layout ("V(7)=H, V(12)=HE", `species_mix_weights(12)`) is defined by `core/read_starting_model.f90`. Add an `integer, parameter` index block in the module that owns that layout (star_info_lib if the array is a `star%` member, else read_starting_model's module) and use it in read_starting_model and at `kap/laol89/rdlaol.f90:50-51` (**shared, hunk-disjoint with `ioctrl`: only those subscripts**). If the layout is only partly understood, name only the elements you can prove and leave the rest literal.

### Files you may edit

`src/state/star_info_lib.f90`, `src/state/phys_const_lib.f90`, `src/net/net_lib.f90`, `src/core/burn_lib.f90`, `src/core/henyey_iterate.f90`, `src/core/henyey_coefficients.f90`, `src/core/evolve_step.f90`, `src/core/run_yrec.f90`, `src/core/main.f90`, `src/core/read_starting_model.f90`, `src/core/rebuild_envelope.f90`, `src/core/observables_lib.f90`, `src/core/neutrino_flux_table.f90`, `src/core/monte_carlo.f90`, `src/util/compute_timestep.f90`, `src/util/timestep_limit_hburn.f90`, `src/mixing/mix.f90`, `src/mixing/find_convection_zones.f90`, `src/setup/locate_shell_boundaries.f90`, `src/setup/setups.f90`, `src/wind/massloss.f90`, `src/deps.mk`.
Shared, hunk-disjoint: `src/rotation/microdiff/gravitational_settling_setup.f90`, `src/rotation/microdiff/microdiff_coefficients.f90`, `src/rotation/microdiff/microdiff_setup.f90`, `src/rotation/viscos.f90` (item 4 literals only), `src/kap/laol89/rdlaol.f90` (item 10 subscripts only).
If `star_info_lib`'s `controls_state` include files or `state/controls_sync_lib.f90` need regenerating you are in `ioctrl`'s territory -- none of your items should need it; if one does, defer it.
