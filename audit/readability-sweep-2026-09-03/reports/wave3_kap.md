## NAME = kap

Worktree `/Applications/YREC-wt/kap`, branch `rs/kap`. This is batch **R5 (structural)** for the opacity/atmosphere-table domain. Read first: audit `kapatm.md` ("Structural recommendations" 1, 3, 4; finding 6), `reports/kapatm-wave1.md` (Skipped: "`_z2` clone merges"), `reports/ioctrl-wave2.md` (what moved into `star%ctrl` / what was renamed), `SUMMARY.md` section 3 (R5).

You own `src/kap/**`, `src/atm/**` and -- for the two shared kernels of item 3 only -- `src/numerics/numerics_lib.f90`. Nobody else edits numerics_lib in this wave. `src/io/controls_lib.f90` and its generated `state/` files are off-limits (namelist-facing); if a table-set type needs a unit number or path that lives in `star%ctrl`, read it from there, do not move it.

### Items, in order

1. **Table-set derived types, one per clone family** (kapatm Structural 1). Today `opacity_table_lib.f90` carries `kurucz_*` and `kurucz2_*`, `laol_*`/`slaol_*` and `laol2_*`/`slaol2_*`, `opal92_*` and `opal92_*_z2` member sets, and `atm_table_lib.f90` carries `kurucz_teff_table`/`atm_table_nt` and `kurucz_castelli_teff_table`/`atm_table_ntc`; the interpolators `kurucz`/`kurucz2`, `gtlaol`/`gtlaol2`, `opal92_interp2d`/`_z2`, `opal92_interp3d`/`_z2`, `surfp`/`kcsurfp` are near-clones that differ in which member set they address. For each family:
   - `diff -w` the pair first and list every non-rename difference. Known: `kurucz.f90` has a `kurucz_logrho_max/kurucz_logt_max` range check that `kurucz2.f90` lacks; `opal92_interp3d.f90` has an X-grid `findex` block that the `_z2` copy lacks; `surfp`/`kcsurfp` differ in table length parameters. A merged routine must reproduce each caller's exact behaviour: either the extra block is gated on a component of the type set only for the instance that had it (and the gate is a plain `if`, evaluated where the block was), or the pair is not merged.
   - Define `type kurucz_table_set` (grid, index cursor, density-start index, opacity arrays, counts), `type laol_table_set`, `type opal92_table_set`, `type surface_p_table` in the owning `*_table_lib.f90`, with components named after the existing `(1)` set's members (drop the `2`/`_z2` suffix). Replace the two member sets by two instances (`opacity_table%kurucz(2)`, or two named instances if array-of-type would change any indexing) and give each interpolator/reader/spliner a `type(...), intent(inout) :: tbl` argument. Callers (`kap_lib.f90 kap_eval`, `setupopac.f90`, the `read_*`/`build_*` setup routines) pass the instance the old routine addressed.
   - Delete the clone files (`git rm kurucz2.f90 gtlaol2.f90 opal92_interp2d_z2.f90 opal92_interp3d_z2.f90 kcsurfp.f90`) and the mirrored halves of the setup routines only when the retained routine, called with instance 2, executes statement-for-statement what the clone executed. Terminal-only message strings that name the routine (`'opal92_interp2d_z2: CHECK NDSS'`) may become the generic name -- they do not reach pinned files -- but say so in the commit message.
   - The alternate returns (`return 1`) stay as they are (R6 territory); do not change error conventions.
   - `save` semantics: the table sets are module variables today; keep them module variables of the new types.
   Do this one family at a time (kurucz, then laol, then opal92, then surface_p), each its own commit with gate1 IDENTICAL, so a pin difference in one family is isolated.

2. **`kap_table_dims` parameter block** (kapatm Structural 4). Wave 1 added `n_opal95_tables`, `n_laol_x/rho/t`, and the missing-value sentinels to `opacity_table_lib.f90`. Finish it: one `integer, parameter` block per family (opal95 `nt/nd/nx/nz/n_slots`, opal92, alex94, alex06, kurucz, laol) at the top of `opacity_table_lib.f90` (and the atm table lengths in `atm_table_lib.f90`), declare the arrays from them, and replace the per-file literal copies (`57/11/76`, `12/104/52`, `70/19/10/13`, ...) in `kap/**` where the literal is provably that dimension (an array bound, a loop bound over that axis, a `findex`/`locate` length argument). Leave a literal that is a physical value or an offset. Verify the ll95tbl 126-vs-130 note in the audit: if the two numbers are both real (table count vs slot count) give both names; do not "fix" the mismatch.

3. **Shared stencil kernels in `numerics_lib`** (kapatm Structural 3, finding 6) -- only the byte-safe part:
   - `lagrange4` already exists (`pure function lagrange4(w, y)`, `w(1)*y(1)+w(2)*y(2)+w(3)*y(3)+w(4)*y(4)`). Replace the inline 4-term weighted sums in `opal95_interp{2,3,4}d`, `getalex06.f90`, `alex94_interp3d.f90`, `alex06tab.f90`, `opal95_fixed_z_table.f90` with `lagrange4(w, y(i0:i0+3))` ONLY where the inline sum is `w1*a + w2*b + w3*c + w4*d` in exactly that left-to-right order with a contiguous `y` stride. A sum written `a*w1 + ...`, or with a different association, or over a non-unit stride that would need a temporary, stays inline (say so in the report).
   - `stencil4_locate`: the 13 hand-rolled 4-point window searches (finding 6) differ in cursor handling and clamping. Write the helper only if at least three sites are token-identical after renaming; otherwise skip the helper and list the sites with their differences under Deferred.

4. **`opal95` hidden-argument chain** (kapatm Structural 2, the class-A slice only): `opal95_interp2d/3d/4d` take no inputs and read `opacity_table%opal95_logr/weight_*/index_*` set in `getopal95`. Give them explicit `intent(in)` arguments for the values they read and pass them from `getopal95` (same values, same names). Do not change the alternate-return/error conventions or `kap_eval`'s dispatch (that is the larger Structural 2, deferred).

### Files you may edit

`src/kap/**`, `src/atm/**`, `src/numerics/numerics_lib.f90` (item 3 only), `src/tools/check_boundaries.py` (minimal allowlist edits only), tests under `src/kap/test`, `src/atm/test`, `src/deps.mk`.
