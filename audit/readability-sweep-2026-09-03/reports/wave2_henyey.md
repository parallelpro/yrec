## NAME = henyey

Worktree `/Applications/YREC-wt/henyey`, branch `rs/henyey`. This is batch **R4 (explicit data flow)** for the core/atm chain. Read first: audit `core.md` (items 3, 4, 5, 14, 15 and "Structural" 2-3), `reports/core-wave1.md` (Deferred 6, 8, 9), `SUMMARY.md` section 3 (R4), and `src/GUIDELINES.md` (the kernel "star-blind" contract for envint_kernel / bsstep / derivs).

Another agent (`sidechan`) owns `src/mixing/*`, `src/setup/rezone.f90`, `src/rotation/microdiff/*`, `src/util/compute_timestep.f90`, `src/wind/mdot.f90`, `src/eos/*`. Its `temperature_gradients` `zone_index` argument changes the *callers* in `core/henyey_coefficients.f90`, `core/shell_physics.f90`, `core/envelope_derivs.f90` -- those three files are **shared, hunk-disjoint**: it edits only the `call temperature_gradients(...)` lines and the `star%iovim` assignments feeding them; you edit nothing on those lines.

### Items, in order

1. **Henyey flag relay -> locals** (core 3). In `core/henyey_coefficients.f90` the flags set to constants at the bottom (`in_atmosphere`, `want_derivatives`, `mixing_active`, `conductive_opacity_flag`, ...) and declared `intent(out)` are relayed to SAVEd driver locals in `core/henyey_iterate.f90` / `core/evolve_step.f90` that nobody reads (`star%converged_zone(im) = conductive_opacity_flag` at evolve_step ~405 is always `.true.`). Show each relayed value unread (grep every reader of the driver local and of `star%converged_zone`), then make them locals/parameters of henyey_coefficients and drop them from the signatures. If `star%converged_zone` is read anywhere (io/ output?), keep it assigned `.true.` explicitly and say so.
2. **Delete `kenv`/`katm`** (core 4): zeroed and incremented in henyey_iterate, rebuild_envelope, read_starting_model; threaded surfbc -> atm_get (core/envint_lib) -> envint_kernel -> numerics bsstep -> derivs (core/envelope_derivs); only "consumer" is a commented-out format at surfbc ~243. Remove from all six signatures and the three drivers. `src/numerics/*.f90` is yours for this item only (bsstep's dummy list); `make clean` after the signature changes.
3. **`atm_get` signature** (core 5): 22 positional args incl. four `atm_get_dummy*` arrays and `atm_get_unused_flag`, and three call sites (`stitched_model`, `rebuild_envelope`, `read_starting_model`) that save/override/restore `star%job%env_step_{max,min,begin}` to smuggle `chi_grid_scale(8)`. Give `atm_get` an optional `envint_config` argument (a small derived type in envint_lib holding the three step values) that replaces the save/override/restore blocks, and make the vertex outputs optional so the dummy arrays go away. The values flowing into envint_kernel must be exactly the ones the override wrote today; note that `read_starting_model` returns at ~870-873 without restoring the step sizes -- reproduce that (the later code sees the overridden values) or show nothing downstream reads them before the next write. Do not change `envint_kernel`'s arithmetic.
4. **Sign-encoded flags** (core 14): `star%dt = -dabs(star%dt)` as "do not evolve" (evolve_step ~361, 141) and `fcorr = dabs(fcorr0) - fcorri` with the sign of `fcorr0` as a mode (henyey_iterate ~336). Only if a named logical can carry the mode with every arithmetic use of the value unchanged (the `dabs` must stay where a magnitude is used); otherwise defer with the exact proposal.
5. **Small core items** (core 15): `use_correct_gradients = .true.` never read (delete, show unread); the retired-machinery comment in `update_output_flags_for_step` (rewrite to what it does now); the duplicated pressure-scale-height formula (evolve_step ~336-337 vs ~541-542) into one contained function only if token-identical.
6. **`luminosity_breakdown` renormalisation** (core-wave1 Deferred 9): `henyey_iterate` guards on `total .gt. 0`, `observables_lib` does not. Do NOT merge them; add a one-line comment at each pointing at the other and at the report.

### Files you may edit

`src/core/henyey_iterate.f90`, `src/core/henyey_coefficients.f90` (not the `temperature_gradients` call lines), `src/core/evolve_step.f90`, `src/core/envint_lib.f90`, `src/core/envint_kernel.f90`, `src/core/envelope_derivs.f90` (not the `temperature_gradients` call lines / `iovim` lines), `src/core/shell_physics.f90` (same restriction), `src/core/surfbc.f90`, `src/core/stitched_model.f90`, `src/core/rebuild_envelope.f90`, `src/core/read_starting_model.f90`, `src/numerics/*.f90` (item 2 dummy lists only), `src/deps.mk`.
