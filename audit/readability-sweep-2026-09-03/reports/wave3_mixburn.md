## NAME = mixburn

Worktree `/Applications/YREC-wt/mixburn`, branch `rs/mixburn`. This is batch **R5 (structural)** for the mixing + burn/net domains. Read first: audit `mixwind.md` ("Structural recommendations" 1, 2; items on mix/rotmix), `burn.md` ("Structural" 1, 4, 5), `reports/mixwind-wave1.md`, `reports/burn-wave1.md` (R3: `locate_cz_base`/`average_cz_abundances` already extracted; Deferred 3), `reports/state-wave2.md` (`star%reaction_rate(13,json)` and the named counts), `reports/sidechan-wave2.md` (item 5: the `star%job%` flag flips it prepared; the rotmix gradient argument), `SUMMARY.md` section 3 (R5).

You own `src/mixing/**`, `src/core/burn_lib.f90`, `src/net/**`, `src/util/timestep_limit_hburn.f90`, `src/util/timestep_limit_heburn.f90`, `src/util/compute_timestep.f90`, `src/wind/mdot.f90`. `src/rotation/rotation_scratch_lib.f90` is **shared, hunk-disjoint** with the `rotation` agent: you may edit only the `reaction_rate_by_zone` declaration/comment and the `rr_*` index lines. `core/evolve_step.f90` is the `core` agent's; `core/henyey_coefficients.f90` and `core/neutrino_flux_table.f90` call `engeb` -- for item 2 you may change only their `call engeb(...)` statements and the declarations of the actuals those calls pass (shared, hunk-disjoint with `core`). The BS-extrapolation path (`burn_settle_mix.f90`, `burn_mix_extrapolated.f90`) and `alfmlt/phmlt/cmxmlt` are author decisions: leave their contents alone except where item 1 must touch a `rates` call there.

### Items, in order

1. **Nuclear-rate relay as one vector** (mixwind Structural 1). `net_lib` `rates(...)` writes fifteen per-zone arrays (`rate_pp(json)`, ..., `rate_zero13(json)`, `frac_c12_alpha(json)`, `frac_be7_electron(json)`) at element `zone_idx`; `mix.f90` copies them into `rot_scr%reaction_rate_by_zone(1..15, zone_idx)` in that same order; `rotmix.f90` copies them back out by `rr_*` row; `timestep_limit_hburn/heburn.f90` and `burn_mix_extrapolated.f90` call `rates` too; `eqburn` takes them positionally. Make `rates` write `rate(1:15)` for one zone (or `rate_by_zone(:, zone_idx)`, whichever keeps every caller's actual contiguous -- state which), give `eqburn` (and any other positional consumer) a `rate(:)` argument addressed by `rr_*`, and fill `rot_scr%reaction_rate_by_zone(:, k)` by one slice assignment. Rules: each element is the same value computed in the same statement order inside `rates` (rename the local scalars, do not merge expressions); every reader indexes the row that held the same quantity before; `rate_zero9`/`rate_zero13` are always zero -- keep the rows (readers may read them) and say in a comment that they are zero-filled placeholders; the `rr_*` names in `rotation_scratch_lib.f90` are the single index scheme (add the missing ones, e.g. `rr_zero9 = 9`, `rr_zero13 = 13`, `rr_frac_be7_electron = 15`, if absent). `star%reaction_rate(13,json)` (state, wave 2) is a different array (engeb's rates, gyr units) -- leave it. One commit per caller family is fine; gate1 each.

2. **`engeb` output record** (burn Structural 4): the ~20-argument `engeb` signature. Define `type burn_result` in `burn_lib` (eps by chain, `deps_dlnT`, `deps_dlnrho`, `eps_nu`, `neutrino_flux(n_nu_fluxes)`) and have `engeb` fill it; the three callers (`henyey_coefficients`, `neutrino_flux_table`, and whichever third one grep finds) read the components. Arithmetic inside `engeb` does not move (the bit-drift note at its inline rate block forbids extraction -- do not touch that block). Only if every caller's downstream expression reads the same value it read before; the split of `compute_neutrino_emission` into three internal subroutines is allowed only as pure cut-and-paste in original statement order.

3. **`dburn`/`dburnm` interface note** (burn Structural 5): header table of the four documented differences (units, threshold, weighting, rate source); the `timestep_gyr` conversion and the two thresholds into named parameters (`double precision, parameter`, exact literal value and kind). Do not unify the bodies (class B).

4. **Callee-side `star%job%` writes** (sidechan-wave2 item 5): if the `core` agent applied the driver-side flip in `evolve_step.f90` (their report says so), remove the `star%job%...` write in `util/compute_timestep.f90` / `wind/mdot.f90` and return the logical sidechan prepared. You cannot see the core branch: do this item last, and only if `reports/core-wave3.md` exists and confirms the driver edit; otherwise leave the write in place and say so.

5. **Burn-region iterator** (mixwind Structural 2) -- only if provable. `mix.f90`'s three burn loops (and `rotmix.f90`'s) walk convective/radiative regions and call `solve_composition` per region. Write `build_burn_regions(...)` producing the ordered (begin, end, is_convective) list and replace the loops ONLY if you can show, by instrumenting a debug build (print the (begin,end) sequence before and after on the solar pin and on `run_config_matrix`'s rotating cases, then delete the prints), that the sequence of `solve_composition` calls and their arguments is identical. If you cannot, defer with what you found (the "single-shell CZ treated as radiative unless it is the centre" rule and its three sites).

6. **Reaction table** (burn Structural 1) -- optional, last: `type reaction_data` parameter array (Q1-Q8, zprd/z53/z43/z23/z86, eg, S0 slot) replacing the parallel `data` statements in `net_lib`/`burn_lib`. Generate the table entries mechanically from the existing `data` lines (script in your scratch, not committed), then add a check to `net/test/test_net.f90` that prints every old and new value and compare them exactly. Only if `eg`/`rate` reads stay element-by-element in the same loops. If time is short, put the mechanical proposal under Deferred instead.

### Files you may edit

`src/mixing/**` (BS path: only `rates` call lines), `src/core/burn_lib.f90`, `src/net/**`, `src/util/timestep_limit_hburn.f90`, `src/util/timestep_limit_heburn.f90`, `src/util/compute_timestep.f90`, `src/wind/mdot.f90`, `src/tools/check_boundaries.py` (minimal allowlist edits only), tests under `src/net/test`, `src/mixing/test` if any, `src/deps.mk`.
Shared, hunk-disjoint: `src/rotation/rotation_scratch_lib.f90` (`reaction_rate_by_zone` / `rr_*` lines), `src/core/henyey_coefficients.f90` and `src/core/neutrino_flux_table.f90` (`call engeb` statements and the actuals' declarations only).
