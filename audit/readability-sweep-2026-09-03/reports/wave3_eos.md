## NAME = eos

Worktree `/Applications/YREC-wt/eos`, branch `rs/eos`. This is batch **R5 (structural)** for the equation-of-state domain. Read first: audit `eos.md` ("Structural recommendations" 1-3, 5; findings 1, 5, 6, 9, 10), `reports/eos-wave1.md` (Skipped: item 2 quad dedupe "needs the cache derived type first"; Deferred), `reports/sidechan-wave2.md` (what it did in `eqstat.f90`/`mu.f90`/`eos_mixture_lib.f90`), `SUMMARY.md` section 3 (R5).

You own `src/eos/**`. `src/eos/eos_lib.f90` is the domain's public entry: keep its public interface names unchanged (callers in core/, atm/, mixing/, wind/ use `eos_get`, `eos_set_debye_huckel_z`, ...). Alternate returns, `-999` sentinels and `return 1` conventions stay (Structural 4 is R4-style control-flow change, deferred by the wave-1 agent; do not do it here).

### Items, in order

1. **`opal_eos_vintage` derived type** (eos Structural 1). `opal_eos_lib.f90`'s `opal_eos_state` holds three parallel member sets: unsuffixed (1995), `_01`, `_06` (tables, x/z grids, index arrays, `quad*` caches, `rho_interp_*`, `eos_output`, init flags, `table_metal_fraction*`). Define `type opal_eos_vintage` with the unsuffixed names and the per-vintage dimensions as components sized by the largest (`n_eos06_nr/nt`) with explicit `nr`, `nt`, `nvar` count components -- ONLY if every loop and `findex`/`locate` length today uses the per-vintage count (so a larger physical array changes nothing); if some loop runs over the declared bound, keep three separately-dimensioned types or parameterise differently, and say which. Instances `opal95`, `opal01`, `opal06` inside `opal_eos_state`. Then, one vintage-clone family at a time:
   - `quad`/`quadeos01`/`quadeos06` (wave-1 item 2): `diff -w` them; if they differ only in the cache member names, keep one `quad` taking `type(opal_eos_vintage), intent(inout) :: v`; `git rm` the two clones.
   - `t6rinterp`/`t6rinteos01`/`t6rinteos06`, `eqbound`/`eqbound01`/`eqbound06`, `gmass`/`gmass01`/`gmass06`, `rhoofp`/`rhoofp01`/`rhoofp06`, `radsub`/`radsub01`/`radsub06`: same test, same rule. The audit notes `rhoofp01` has a private edge table differing from the module's and `esac06` calls `radsub06` with an amu/log10ne column difference -- a difference like that means NOT merged; parameterise only the identical part (or leave that clone) and list the difference in the report.
   - `esac*`/`readco*`/`oeqos*` stay as thin vintage-specific wrappers (their differences are real: column layouts, Z formats). Give them the instance argument so they stop addressing `_01`/`_06` members by name, nothing more.
   Each family is its own commit with gate1 IDENTICAL. Names: components lose the suffix; the index parameter blocks `i_opal_*` vs `i_opal01_*` (different slot layouts) stay as they are and are documented as the layout descriptor of each vintage.

2. **`blend_opal_result` out of `eqstat2`** (eos Structural 2). `eqstat.f90` (`eqstat2`, ~lines 600-810 pre-wave-1; grep for the three ramp/blend blocks) repeats the vintage-blend arithmetic per vintage. Extract `subroutine blend_opal_result(...)` only if the three blocks are token-identical after substituting the vintage's `eos_output`/gate values; keep the T6 gates as three named parameters or a 3-element array with a comment giving the meaning of each. The beta14 convention discrepancy noted in the audit is a known item -- do NOT fix it; keep the exact behaviour per vintage (if the blocks differ because of it, they are not identical: extract the identical remainder or skip).

3. **Composition-indexed MHD state** (eos Structural 3). `mhd_eos_lib.f90` holds `zams_lower_a_table`, `zams_upper_a_table`, ..., `centre1_table` ... `centre5_table` as named members; `mhdpx2.f90` is an 11-way `if` ladder of `interpolate_and_store` calls over them; `mhdst.f90` calls `mhdst1` eight times with unit numbers `unit_zams_a/b/c`, `unit_centre1..5`. Replace the named members by arrays indexed by a small `integer, parameter` block (`imhd_zams_a_lo = 1, imhd_zams_a_hi = 2, ..., imhd_centre1 = 7, ...`) and turn the ladder and the eight calls into loops over a unit/path array -- provided (a) the loop order equals today's statement order, (b) each `mhdst1`/`intpt` call still receives a contiguous slice with the same explicit-shape lengths (`rtab` sequence association; pass `tables(:,:,:,k)` or an element-based actual as today), and (c) the `table_selector` dispatch reproduces exactly which tables were used for each selector value. The unit-number arguments of `mhdst` come from the caller (`io/read_controls.f90` sets 40-47 -- ioctrl left them); keep the `mhdst` interface accepting the same eight units (or an 8-array built by the caller inside `eos_lib` only if the caller is in eos/).

4. **Named indices for the remaining packed arrays** (eos Structural 5): `mhd_output(25)` slots in `mhd_eos_lib`, the SCV table columns in `scv_eos_lib`, and the 12-species mixture order in `eos_mixture_lib` (`fxenv`, `atomic_weights_full`, `saha_mass_fractions`). `integer, parameter` blocks with a one-line comment per slot, used at the literal subscript sites in eos/**. Only name a slot whose meaning you can prove from a writer or reader; leave others literal. Do not touch `core/read_starting_model.f90` (it defines the 12-element layout too; the `state` agent added an index block there in wave 2 -- reuse its names if they are in a module you may `use`, else list the duplication under Deferred).

### Files you may edit

`src/eos/**`, tests under `src/eos/test`, `src/tools/check_boundaries.py` (minimal allowlist edits only), `src/deps.mk`.
