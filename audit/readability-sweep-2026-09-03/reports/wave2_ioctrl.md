## NAME = ioctrl

Worktree `/Applications/YREC-wt/ioctrl`, branch `rs/ioctrl`. Read first: `reports/kapatm-wave1.md` (Deferred 1-8), `reports/mixwind-wave1.md` (items 3, 5, 10), `reports/rotation-wave1.md` (item 1), `reports/burn-wave1.md` (items 3, 5), `reports/eos-wave1.md` (items 1, 2), `reports/io-wave1.md` (the aux-test regex note), and audit `io.md`, `kapatm.md`.

You own `src/io/controls_lib.f90` (and its generated outputs), the namelist reader, kap/atm loaders, and the tools. `src/io/controls_lib.f90` is the source of truth for `star%ctrl`: after ANY change to its member list or names run `cd src && python3 tools/gen_controls_state.py` and commit the regenerated `state/controls_state_def.inc`, `state/job_controls_def.inc`, `state/controls_sync_lib.f90` together with it (these are the only `state/` files you may touch; `state` agent owns `star_info_lib.f90`/`phys_const_lib.f90`). **Namelist variable names must not change** (input files depend on them); renaming a `star%ctrl%` member is fine only if the namelist name is a separate string -- check how `read_controls`/`gen_controls_state.py` map them before renaming anything, and skip a rename that would alter what an inlist accepts.

### Items, in order

1. **Unit renames in controls_lib** (kapatm 1, 2): `laol_table_unit` -> `opal92_table_unit` (only user `kap/opal92/read_opal92_tables.f90`), `ikur2` -> `kurucz_table2_unit` (`kap/kurucz90/read_kurucz_tables.f90`). Regenerate the sync module.
2. **Hard-coded `iolaol = 61`, `iopurez = 62`** (kapatm 3): `open(newunit=...)` in `kap/laol89/rdlaol.f90` / `rdzlaol.f90` if the unit is not referenced anywhere else after the open (grep); delete the two assignments in `io/read_controls.f90` and the bare variables in `kap/opacity_table_lib.f90`. If a unit number leaks into any output, skip.
3. **`tollaol` / `llaol` / `use_pure_z_table` into `star%ctrl`** (kapatm 4): they are namelist inputs held as bare `opacity_table_lib` variables and read/written from `io/read_controls.f90`, `io/read_yrec7.f90`, `io/read_model2.f90`, `kap/*`, and `core/read_starting_model.f90` (**shared, hunk-disjoint with `state`**: only the lines that reference these three names). Do it only if the write sites are all in the read path (no physics routine mutates them after the read -- `star%ctrl` is immutable after read); otherwise defer with the reason.
4. **`rdlaol` writes `debye_huckel_z`** (kapatm 5, eos yale_eos_lib): read into a local in `rdlaol`, return it to `kap/setupopac.f90`, which hands it to eos through a new one-line public setter in `src/eos/eos_lib.f90` (e.g. `eos_set_debye_huckel_z(z3)`); update `tools/check_boundaries.py` allowlist minimally if needed. The value must reach `debye_huckel_z` before any use, exactly as today (check the call order).
5. **`setupopac` sets `star%use_two_z_tables`** (kapatm 6): move the decision `star%use_two_z_tables = star%ctrl%use_z_ramp .or. star%job%use_diffusion_z` to the core/ site that resolves `use_z_ramp`/`use_diffusion_z` (find it; likely `core/star_setup.f90` or `run_yrec.f90` -- **shared, hunk-disjoint with `state`** if it is a file in its list; one moved line only), and leave setupopac reading it. Verify order: the value must be set before setupopac runs and before anything else reads it.
6. **`alex95` -> `alex94`** (kapatm 7): rename `n_alex95_*`/`alex95_*` in `kap/opacity_table_lib.f90`, `kap/alex94/*`, `kap/setupopac.f90`, `kap/surfopac.f90`, `kap/kap_lib.f90`, tests under `kap/test`, `atm/test`, `eos/test`, `net/test` if they mention it, the `star%ctrl` member `alex95_table_unit` (if the namelist name is a separate string) -- but the namelist variable `lalex95` stays. `setup/setups.f90` and `star_info_lib.f90` mention alex95 too: setups.f90 you may edit; for star_info_lib list the line under Deferred.
7. **`max_runs = 50`** (mixwind 3): parameter in controls_lib for the `(50)` run arrays; use it in `setup/setup_star_calibration.f90` and `setup/setup_solar_calibration.f90` (`50` / `48` -> `max_runs` / `max_runs-2`). Regenerate.
8. **`chi_grid_scale` slot names** (rotation 1, mixwind 5): `ichi_dm_max = 2, ichi_dl_max = 9, ichi_dp_core_max = 11` (verify each against `setup/map_user_inputs.f90:203-209`) plus names for the slots read at `core/read_starting_model.f90`, `core/rebuild_envelope.f90` (**shared, hunk-disjoint with `state`**), `rotation/equal_spaced_grid.f90`, `rotation/seculr/am_transport_grid.f90`, `rotation/seculr/composition_grid.f90` (**shared with `rotmix`: only those subscripts**), `setup/rezone.f90` (**shared with `rotmix`**). Same for `atime(1..14)` slots read in `util/timestep_limit_*.f90`, `util/compute_timestep.f90`, `setup/locate_shell_boundaries.f90`, `setup/map_user_inputs.f90`: name only the slots whose meaning map_user_inputs makes explicit.
9. **`cross_section_scale(14..17)`, `qs0e_scale(8)`, `qqs0ee_scale(8)`** in io/ (burn 3, 5): use `s_pep`, `s_be7e`, `s_be7p`, `s_hep`, `iq_be7p` from `net_lib` at the sites in `io/read_controls.f90` / `setup/map_user_inputs.f90` if io/ may `use net_lib` under `tools/check_boundaries.py`; else defer.
10. **`tools/check_boundaries.py:89`** stale "and wind/wcz" comment (mixwind 10) -- drop it.
11. **`eos_get_gamma1`** (eos 2): test-only; mark it so in `eos_lib`'s public line comment and, if the allowlist in check_boundaries.py lists it as a production entry, move it to a test-only note. Do not delete it.
12. **`mhdst` centre-table units hard-coded 40-47 at `io/read_controls.f90:1431-1438`** (eos 1): only if those assignments are provably equal to the declared defaults of the same members (so dropping them changes nothing for any inlist); otherwise leave and document in the report.
13. **GUIDELINES note**: add one sentence to `src/GUIDELINES.md` (testing section) that `test_mesa_output.py::test_default_columns_lists_in_sync` regex-harvests literal `names(<digits>) = '...'` lines in `io/profile_output.f90`, so those stay literal.

### Files you may edit

`src/io/**/*.f90`, `src/state/controls_state_def.inc`, `src/state/job_controls_def.inc`, `src/state/controls_sync_lib.f90` (generated only), `src/kap/**`, `src/atm/**`, `src/eos/eos_lib.f90`, `src/setup/map_user_inputs.f90`, `src/setup/setup_star_calibration.f90`, `src/setup/setup_solar_calibration.f90`, `src/setup/setups.f90`, `src/util/timestep_limit_*.f90`, `src/util/compute_timestep.f90` (item 8 subscripts only -- **shared with `state`**, which renames `hydrogen_dt` there), `src/setup/locate_shell_boundaries.f90` (item 8 subscripts only -- **shared with `state`**), `src/tools/check_boundaries.py`, `src/GUIDELINES.md`, `src/deps.mk`, tests under `src/*/test/`.
Shared, hunk-disjoint (named subscripts / one moved line only): `src/core/read_starting_model.f90`, `src/core/rebuild_envelope.f90`, `src/core/star_setup.f90` or `src/core/run_yrec.f90` (item 5), `src/rotation/equal_spaced_grid.f90`, `src/rotation/seculr/am_transport_grid.f90`, `src/rotation/seculr/composition_grid.f90`, `src/setup/rezone.f90`.
