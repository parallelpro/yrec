## NAME = rotmix

Worktree `/Applications/YREC-wt/rotmix`, branch `rs/rotmix`. Read first: `reports/rotation-wave1.md` (items 2, 5), `reports/mixwind-wave1.md` (items 4, 6), `reports/io-wave1.md` (Deferred: lagrange4), and audit `rotation.md`, `mixwind.md`.

You own `src/rotation/rotation_scratch_lib.f90` and the rotation<->mixing renames that wave 1 could not do inside one domain. Another agent (`state`) owns `src/state/*` and replaces literal physical constants in four rotation files (`microdiff/gravitational_settling_setup.f90`, `microdiff/microdiff_coefficients.f90`, `microdiff/microdiff_setup.f90`, `viscos.f90`) -- those files are shared, hunk-disjoint: do not touch their constant literals. Another (`ioctrl`) owns `src/io/controls_lib.f90` and will name the `chi_grid_scale` slots used at `rotation/equal_spaced_grid.f90` and `rotation/seculr/am_transport_grid.f90` -- do not touch those two subscripts. Namelist-facing names (`no_am_transport_in_core`/LNOJ etc.) are out of scope.

### Items, in order

1. **`rot_scr%log_luminosity` / `log_luminosity_start` hold linear L/Lsun** (rotation item 2, audit rotation #16). Rename the `rotation_scratch_lib` members and every dummy argument that carries them to `luminosity_lsun` / `luminosity_lsun_start` (pure rename) across rotation/ and the mixing readers `mixing/burn_settle_mix.f90`, `mixing/semiconvection.f90`, `mixing/temperature_gradients.f90`. Be careful: `star%log_luminosity` (a true log10 L) and the wind files' `log_luminosity` locals are different things -- grep each hit and rename only the linear-L ones; state in the commit message how you told them apart.
2. **Named rows for `reaction_rate_by_zone(15,json)`** (mixwind item 4): `rr_h1 = 1, rr_he4 = 2, rr_he3 = 4, rr_c12 = 5, rr_c13 = 6, rr_n14 = 7, rr_o16 = 9` (verify each mapping from the writer of that array before naming it) in `rotation_scratch_lib`; use them in `setup/rezone.f90` (`reaction_rate_species_index = [...]`, **shared, hunk-disjoint with `ioctrl`**, who edits rezone's `atime(...)` subscripts) and wherever the rows are addressed by literal.
3. **`rotation_scratch_lib` legacy member names** (rotation item 5): `facd2/3`, `vesd2/3`, `hs3`, `pm`, `tm`, `tho`, `qwrst`, `wmst`, `fact1-6`, `fgsfj`, `es1`, `dm`, `delami`, `masschg2/3`. Rename only the ones whose meaning you can establish from the code (say how, in a comment at the declaration); leave the others. Moving `masschg2/3` into `star%` needs star_info_lib -- defer with the exact proposal.
4. **microdiff `lagrange4`**: `numerics_lib` now has `pure function lagrange4(w, y)` (io R3) and `microdiff_mte_lib` has its own `lagrange4(fac, a, k0)` (rotation R3); the merge fix 272d1e2 narrowed the `use numerics_lib` imports in `microdiff_mte.f90`/`microdiff_etm.f90` to avoid the clash. Decide whether to delete the microdiff copy in favour of `lagrange4(facinterp, a(k0:k0+3))` from numerics_lib (arithmetic `fac(1)*a(k0)+fac(2)*a(k0+1)+fac(3)*a(k0+2)+fac(4)*a(k0+3)` vs `w(1)*y(1)+w(2)*y(2)+w(3)*y(3)+w(4)*y(4)` -- same operations in the same order, so byte-safe) or keep both with a comment. Prefer deleting the duplicate if the pins agree. The `facinterp(1)*composition(1,k0)+...` sums still inline in microdiff_mte (around the former lines 169-175) may join too if token-identical in order.
5. **`am_convective_regions`/`mid_timestep_model`/`evolve_angular_momentum` literal 12/13 zone-bound dimensions**: `state` adds `max_convective_zones = 12`, `max_radiative_zones = 13` to star_info_lib in its branch -- you cannot see them yet. Do NOT add your own; list the rotation/mixing literal sites (file:line) under Deferred for a follow-up.

### Files you may edit

`src/rotation/**/*.f90` except the constant literals in the four files named above and the two `chi_grid_scale` subscripts; `src/mixing/burn_settle_mix.f90`, `src/mixing/semiconvection.f90`, `src/mixing/temperature_gradients.f90`, `src/mixing/rotmix.f90`; `src/setup/rezone.f90` (item 2 lines only); `src/deps.mk`.
