## NAME = sidechan

Worktree `/Applications/YREC-wt/sidechan`, branch `rs/sidechan`. This is batch **R4 (explicit data flow)** for the `star%`/scratch side channels outside the Henyey chain, plus one eos helper. Read first: audit `mixwind.md` (the "Explicit scratch instead of star% side channels" paragraph and the rezone/rotmix/temperature_gradients items), `rotation.md` (items 9, 17 and Structural 5), `reports/eos-wave1.md` (Deferred 4), `reports/mixwind-wave1.md`, `SUMMARY.md` section 3 (R4).

Another agent (`henyey`) owns `src/core/*` and `src/numerics/*`. Three core files are **shared, hunk-disjoint** with it: `core/henyey_coefficients.f90`, `core/shell_physics.f90`, `core/envelope_derivs.f90` -- you may edit ONLY the `call temperature_gradients(...)` lines and the `star%iovim = ...` assignments that feed them (item 2); nothing else in those files. The `star%job%` flag flips (item 5) need the driver in `core/evolve_step.f90`, which is henyey's file: do the callee side and put the exact driver-side change under Deferred.

### Items, in order

1. **`rezone` local spline buffers** (mixwind): `setup/rezone.f90` uses `star%logP_start/logT_start/logRho_start/old_shell_mass` as spline scratch. Replace with local (or module-scratch) arrays of the same shape; verify with grep that nothing between the first scratch write and the routine's exit reads those `star%` members expecting the scratch values, and that nothing after expects them to hold the scratch (if something does, that is a hidden dependency: keep the side channel, document it, defer). `-finit-local-zero` means a fresh local reads 0 where the `star%` array may have held stale data -- check every element read is written first on every path.
2. **`temperature_gradients` `zone_index` argument** (mixwind): `mixing/temperature_gradients.f90:~139-142` reads `star%iovim`, set by `core/henyey_coefficients`, `core/shell_physics`, `core/envelope_derivs` (as `im`) or `-1` by `core/read_starting_model`; `mixing/semiconvection.f90` calls it without setting it. Add an explicit `zone_index` argument carrying exactly what `star%iovim` held at each call (for semiconvection: pass `star%iovim` itself so today's value flows -- and say so in a comment; do not "fix" it). The kernel path (`envelope_derivs`) must stay explicit-args (GUIDELINES star-blind contract). Keep the `star%iovim` writes if any other reader exists (grep); otherwise delete them (`read_starting_model` is henyey's file: if its `star%iovim = -1` becomes dead, list it under Deferred).
3. **`rotmix` gradient argument** (mixwind): `mixing/rotmix.f90:~192-198,258-260` overwrites `star%gradT` with `mix_scr%delm` and restores it to pass the gradient into microdiff/gravitational_settling. Add an explicit gradient-array argument through `rotation/microdiff/*` (whatever reads `star%gradT` there, e.g. `microdiff_mte.f90`'s `eq%del_grad(i) = lagrange4(facinterp, star%gradT, k0)`) and drop the swap. Every reader must receive the same array it read via `star%gradT` before (in rotmix's call: `mix_scr%delm`; in any other caller: `star%gradT`).
4. **Microdiff `*_bl` local copies** (rotation 9, Structural 5): `microdiff_setup.f90` and `gravitational_settling_setup.f90:~211-220` convert caller arrays (`dlnp_dr`, `mass_grams`/`enclosed_mass`, `timestep`, `total_mass`) to Bahcall-Loeb units in place and restore them in `microdiff_etm.f90:~175-182` / `equal_to_model`. Make the setup routines produce local `*_bl` copies (same products, same order), make the callers' arrays `intent(in)`, delete the restore code, and set `star%bl_*_scale` in one place. Byte-safe only if every downstream read of the converted array now reads the `_bl` copy and the restore was exact (x*s/s is NOT exact in floating point -- if the restore was `a = a/scale` after `a = a*scale`, then today's post-restore values are *already* perturbed and the callers currently see perturbed values; in that case keeping intent(in) arrays would change numbers: check this before starting, and if so, defer as class B with the evidence).
5. **Physics routines flipping `star%job%` flags** (mixwind): `util/compute_timestep.f90:~84` and `wind/mdot.f90:~293`. Callee side: return a logical ("disable structure limits" / "disk exhausted") instead of writing `star%job%...`; the driver-side flip belongs in `core/evolve_step.f90` (henyey's) -- write the exact driver edit under Deferred and leave the `star%job%` write in place until then (so nothing changes now); i.e. this item is prepare-only unless the driver is in your list.
6. **eos `ion_mean_weight_excess`** (eos-wave1 Deferred 4): `eos/eqstat.f90` and `eos/mu.f90` share the ion-mean-weight block; a `subroutine ion_mean_weight_excess(x, z, mu_ion_inv, dfx1, dfx12, dfx4, use_envelope)` in `eos/eos_mixture_lib.f90` with the exact statement order of mu.f90, then eqstat reads the returned dfx values. Only if eqstat's copy is statement-for-statement the same as mu.f90's once the interleaved `saha_mass_fractions` setup is hoisted around it without changing any evaluation order; otherwise defer.
7. **`am_advection_diffusion_coeffs`** (rotation 17): local `json=5000, nmax=8000` shadowing star_info_lib (replace with the module parameters if same values); unused computed locals (`omega_mid_init`, ..., `total_velocity`) deleted after showing unread; `timestep` mutated-then-restored at ~693 and `wind_loss_implicit` leaking to the caller -- fix only if byte-safe by the item-4 argument.

### Files you may edit

`src/mixing/*.f90`, `src/setup/rezone.f90`, `src/rotation/**/*.f90`, `src/util/compute_timestep.f90`, `src/wind/mdot.f90`, `src/eos/eqstat.f90`, `src/eos/mu.f90`, `src/eos/eos_mixture_lib.f90`, `src/io/equal_to_model.f90` (item 4 restore code only), `src/deps.mk`.
Shared, hunk-disjoint (item 2 lines only): `src/core/henyey_coefficients.f90`, `src/core/shell_physics.f90`, `src/core/envelope_derivs.f90`.
